using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateRole : MonoBehaviour {

	// Use this for initialization
	void Start () {
		UIManager.instance.OpenUI(UI.CreateRole);
	}
}
